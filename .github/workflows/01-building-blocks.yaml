name: 01 - Building Blocks
on:
  push:
    branches:
      - main
  workflow_dispatch:
    branches:
      - main
jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run: echo "Hello, World!"
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Successful step
        run: |
          echo "I will succeed"
          exit 0
      - name: say goodbye
        run: echo "Goodbye!"
